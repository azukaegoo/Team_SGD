{
  "name": "One Button App",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "web",
  "workspaceFolder": "/app",
  "forwardPorts": [5000],
  "postCreateCommand": "cp .env_sample .env && pip install -r requirements.txt",
  "postStartCommand": "docker compose up -d"
}